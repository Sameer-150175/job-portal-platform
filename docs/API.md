# API Documentation

## Auth

POST /api/auth/register creates a demo user and returns a JWT.

POST /api/auth/login authenticates a user.

## Domain

GET /api/applications lists records.

POST /api/applications creates a record.

PATCH /api/applications/:id updates a record.

## Analytics

GET /api/analytics returns totals and status breakdowns.
